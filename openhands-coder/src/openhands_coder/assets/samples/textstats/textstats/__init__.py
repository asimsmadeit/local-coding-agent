def most_common_word(text: str) -> str:
    """Most frequent word, lowercased, punctuation ignored, ties broken by
    first occurrence. Raises ValueError on empty or whitespace-only input."""
    raise NotImplementedError
