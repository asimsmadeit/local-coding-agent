I want to to use claude code for my enterprise work, but cannot use because of safety and privacy concerns. I have copilot but it sucks donkey ass.I want to build a localized solution. I want tobuild a local version version of claude code that maintains complete privacy via locally hosted llm models and bedrock api access. I want to agent to be comparable performance (with in reason) to claude code performance and exceed copilot especially for long memory context and codebase. I want to use open source and local hosting in a way that prevents no rpovice security risk. The idea is that using goose coding agent as the workspace orchastrator that can help setup environemnts navigate computer etc and then integrate and actually use openhands as the coding agent for debugging, implementation etc. using bedrock api access paid model for planning but distilled and/or fintuned to actually impleent and fix like qwen coder. I need the agent to be able to learn and maintain user preferences and the persisnt shared memory long contexzt memory shared by both agent so they can easily pick up switch between each other because they have same shared memory that gets updated. I want it too be as youself as possible and should have expected tool calling and mcp if necessaryt. I need your help to think through this idea. Do reashearch see value audit idea and find parts aspect sthat would be good to include that I might be forgetting. If reasonable and worth the effort write out list of implementation steps that would have to be done/. Search online for latest research like hugging face daily papers conference papers and frontier articles to make sure include the latest cutting edge technology to include for best agents. OpenHands and Goose have been added to downloads folder . Make md files with findings and learning and another for the implementation steps for reference.